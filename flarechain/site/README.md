# This folder is generated — do not hand-edit

This is the built, static output of `../dashboard` (a Next.js app), published
here so GitHub Pages can serve it directly at:

https://gidoty.github.io/flarechain/site/

To update it after changing anything in `../dashboard`:

```
cd ../dashboard
npm run build
rm -rf ../site/*
cp -r out/* ../site/
```

Then commit and push the changes in this folder along with your source
changes. This README will get overwritten by that `cp` too — that's fine,
recreate it if you like, it's not required.
