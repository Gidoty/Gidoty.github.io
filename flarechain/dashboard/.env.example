# Copy to .env.local (already gitignored). Only a read-only RPC endpoint is
# needed here — the dashboard never holds a private key, it only reads
# transactions to verify them, never sends any.

RPC_URL=https://rpc-amoy.polygon.technology
